package team.gif.robot;

public abstract class Globals {
    // used as a flag to allow classes to indicate they have control of the CIM motor
    // and the joystick input should be ignored
    public static boolean g_buttonControl;
}
